#!/usr/bin/node
// prints 3 lines but by using an array
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
lines.forEach(myLines);

function myLines(value) {
  console.log(value);
}
